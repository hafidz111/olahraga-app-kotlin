<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Olahraga" parent="Theme.Material3.DayNight">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="Theme.Olahraga" parent="Base.Theme.Olahraga"/>
</resources>